class Solution {
    public int solution(int n) {
        // 필요한 피자의 수를 계산합니다.
        // (n + 6) / 7 는 (n을 7로 나눈 후 올림한 값)과 동일합니다.
        int answer = (n + 6) / 7;
        
        return answer;
    }
}
